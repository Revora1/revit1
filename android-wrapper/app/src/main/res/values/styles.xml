<resources>
    <!-- Base application theme -->
    <style name="Theme.RevItUp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/accent_red</item>
        <item name="colorPrimaryVariant">#dc2626</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status Bar & Navigation Bar Integration -->
        <item name="android:statusBarColor">@color/bg_slate</item>
        <item name="android:navigationBarColor">@color/bg_slate</item>
        <item name="android:windowBackground">@color/bg_slate</item>
    </style>
</resources>
